package com.systemdesign.machinecoding.conferenceroom;

import com.systemdesign.machinecoding.conferenceroom.model.Building;

import java.util.HashMap;
import java.util.Map;

public class ConferenceRoomService {
    private Map<String , Building> buildings;
   /* private UserService userService;

    public ConferenceRoomService(UserService userService) {
        this.userService = userService;
        this.buildings = new HashMap<>();
    }

    public addBuilding(String buildingName, User user){
        if(user.type == )
    }*/
}
